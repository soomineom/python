n = int(input())
member = []

for i in range(n):
    member.append(list(map(str, input().strip().split())))
    member[i][0] = int(member[i][0])
    
member.sort(key = lambda x:x[0])

for k in member:
    print(k[0],k[1])
    
